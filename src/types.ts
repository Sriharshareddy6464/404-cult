// src/types.ts

export interface FeaturedWork {
  id: number;
  title: string;
  description: string;
  tags: string[];
}

export interface Insight {
  id: number;
  title: string;
  content: string;
}